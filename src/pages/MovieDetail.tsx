import React from 'react';
import MovieCard from "../components/MovieCard";
import '../styles.css';

const MovieDetail = () => {
    return (
        <MovieCard />
    );
};

export default MovieDetail;