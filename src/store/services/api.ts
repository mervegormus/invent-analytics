export const createServiceApi = (url: string) => {
    // Burada API hizmetinizi oluşturun veya bağlayın
    // Örnek: Axios veya Fetch kullanabilirsiniz
    return {
        getTodo: async (todoId: string) => {
            // API'den veriyi al ve döndür
        },
        // Diğer API metodları...
    };
};